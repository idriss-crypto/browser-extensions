/* eslint-disable boundaries/no-unknown-files */
// this import is a polyfill for `customElements` which is used by web3modal, without this import extension breaks
import '@webcomponents/webcomponentsjs/webcomponents-bundle';
